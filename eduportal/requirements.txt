Flask==3.0.3
PyJWT==2.9.0
bcrypt==4.2.0
python-dotenv==1.0.1
pytest==7.4.0
Flask-Limiter==3.8.0
Werkzeug==3.0.3
APScheduler==3.10.4
pywebpush==2.0.3
google-auth==2.35.0

# Database — PostgreSQL only
psycopg[binary]==3.2.13
SQLAlchemy==2.0.36
alembic==1.13.3

# S3 / Supabase Storage (optional — only needed when S3_BUCKET is set)
boto3==1.35.36
